import React from 'react'

const Header = ({ headerTitle }: any) => {
    return (
        <div>
            <h1>
                {headerTitle}
            </h1>
        </div>
    )
}

export default Header
